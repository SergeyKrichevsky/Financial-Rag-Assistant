# Финансовый RAG-ассистент — минимальный план доработок под требования экзамена  
**Timestamp (UTC):** 2025-11-05 10:18

---

## 1) Context / Purpose
На скриншоте экзаменатор просит добавить «опциональные» функции:  
- категоризация вопросов;  
- интерактивная аналитика (частые темы, статистика вопросов);  
- визуализации (WordCloud, bar charts);  
- выбор тона ответа (friendly/strict/neutral);  
- PDF summary generator;  
- фидбек от пользователя («было полезно?»).  

Задача: пройти зачёт с **минимальными затратами времени и кода**, оставаясь в рамках текущего репозитория **SergeyKrichevsky/Financial-Rag-Assistant** (Streamlit-UI + два режима: Assistants API и локальный RAG). Подтверждение структуры и README репо — см. ссылки в Sources.

---

## 2) Actions Taken (step-by-step)
1. Просмотрел вашу репу (структура, README, UI на `Interface/app.py`, конфиги, локальный RAG мосты).  
2. Сопоставил список желаемых функций со штатными возможностями **Streamlit** и лёгкими Python-библиотеками.  
3. Сформировал **минимальный инкремент** (MVP-добавления), который даёт все требуемые «галочки» без сложной архитектуры.  
4. Подобрал официальные доки/примерные реализации для быстрого копипаста (Streamlit tabs, download_button; `wordcloud`; `fpdf2`; базовые подсказки по «тону» через промпт-инструкции).  
5. Учитывая, что в README указан **Assistants API**, отметил, что API живёт до середины 2026 и пока менять не обязательно, но это «nice-to-have» пометка на будущее (Responses API).

---

## 3) Decisions Made (with rationale)
- **Логи вопросов+ответов в CSV** (`logs/session_logs.csv`): проще всего, не требует БД; достаточно для аналитики, wordcloud и фидбека.  
- **Категоризация**: сначала **правила/ключевые слова** (мгновенно и офлайн), с опцией «LLM-classification» (одна функция с вызовом модели) — чтобы показать «умную» альтернативу.  
- **Аналитика и визуализации**: таб «Analytics» в Streamlit через `st.tabs()`, гистограммы стандартными `st.bar_chart`/`matplotlib`, WordCloud — библиотека `wordcloud`.  
- **Тон ответа**: radio-переключатель в UI; передаём выбранный тон в system-prompt/инструкции (Assistants/Responses или локальный генератор) — быстро и прозрачно.  
- **PDF summary**: библиотека `fpdf2` + `st.download_button()` — минимум зависимостей и кода.  
- **Фидбек**: две кнопки 👍/👎 под каждым ответом; пишем в тот же CSV (timestamp, query_id, helpful: True/False).  
- **Ограничить объём изменений**: правки только в `Interface/app.py` + маленькие утилиты `analytics/metrics.py`, `analytics/pdf.py`, и `configs/`/`requirements.txt`.  

---

## 4) Results & Current State
Готов детальный **план внедрения** (ниже), который закрывает все пункты экзаменатора. Реализация — это ~150–250 строк чистого кода суммарно, одна-две новые мини-папки и 3–4 новых зависимости. Текущая кодовая база и архитектура позволяют вшить это без рефакторинга.

---

## 5) Parameters & Metadata
- **Environment (target):** Python 3.11+; Streamlit (актуальная ветка 1.x).  
- **Libs to add:** `wordcloud`, `fpdf2` (и при желании `pandas` уже есть/добавить).  
- **Models:** без изменений; тон — через промпт.  
- **RAG:** без изменений; только логирование запросов/ретривов (опционально — писать top-k источники в лог).  

---

## 6) Minimal Feature Plan (what & how)

### 6.1 Категоризация вопросов (Budget / Credit / Mistakes / …)
- **UI/UX:** под полем ввода оставить «плашку» с категорией текущего вопроса.  
- **Лёгкий способ:** правила на словарях/регексах:  
  - `BUDGET = {"budget", "spending plan", "envelope", "50/30/20" …}`,  
  - `CREDIT = {"credit score", "fico", "APR", "debt", "loan" …}` и т.д.  
- **«Умный» способ (опционально):** одна функция `classify_with_llm(text, labels) -> label` с few-shot примерами и temperature=0; fallback — правила.  
- **Интеграция:** при каждом запросе вычисляем категорию и пишем в CSV лог (`timestamp, session_id, question, category, tone, helpful=None`).

### 6.2 Интерактивная аналитика (частые темы, статистика)
- **UI:** `st.tabs(["Chat", "Analytics"])`.  
- **Данные:** читаем `logs/session_logs.csv`.  
- **Метрики:**  
  - топ-N категорий (bar chart);  
  - частота слов по вопросам (после стоп-слов) — для WordCloud;  
  - доля полезных ответов (👍 %) по категориям;  
  - самые частые вопросы (optional).  
- **Streamlit:** `st.tabs()` для переключения; графики через `st.bar_chart` или `matplotlib`; таблицы — `st.dataframe`.

### 6.3 Визуализации: WordCloud + Bar Charts
- **WordCloud:** собираем все вопросы (или ответы), чистим, генерим облако (`wordcloud.WordCloud().generate(text)`), показываем через `st.pyplot`.  
- **Bar Charts:** `category -> count`, `category -> helpful_rate`.

### 6.4 Пользовательский «тон ответа»
- **UI:** `st.radio("Answer tone", ["Friendly", "Neutral", "Strict"], index=1)`.  
- **Где применить:**  
  - **Assistants/Responses API:** добавляем префикс к системной инструкции: «Adopt a {tone} style…»;  
  - **Локальный генератор:** аналогично — перед генерацией добавлять префикс в prompt.  
- **Почему это сработает:** тон — это чисто промпт-фича; менять модель/инфру не нужно.

### 6.5 PDF Summary Generator
- **Содержимое PDF:** заголовок (дата, сессия, выбранный тон), затем последние N Q/A, в конце — краткая сводка (LLM или просто список источников в локальном режиме).  
- **Реализация:** `fpdf2` (10–20 строк): добавляем страницы/шрифт/текст, сохраняем в `BytesIO`, отдаём через `st.download_button("Download PDF", data, file_name="chat_summary.pdf")`.  
- **Опционально:** добавить логотип/поля/номер страницы.

### 6.6 User Feedback («Полезен ли ответ?»)
- **UI:** две кнопки под бот-сообщением: «👍 Да» / «👎 Нет».  
- **Логика:** записать в CSV для текущего `answer_id` → `helpful = True/False`.  
- **Аналитика:** строим метрики полезности по категориям и по тону.

---

## 7) Concrete File Changes (small & scoped)
- `Interface/app.py`  
  - добавить `st.tabs()` → «Chat» и «Analytics»;  
  - radio «Answer tone»;  
  - хук на отправку вопроса: категоризация + запись в `logs/session_logs.csv`;  
  - под ответом: кнопки 👍/👎 → обновление лога;  
  - кнопка «Download PDF» → вызывает `analytics/pdf.py:create_chat_pdf()`.  
- `analytics/metrics.py` (новый)  
  - функции: `load_logs()`, `compute_category_counts()`, `compute_helpful_rates()`, `build_wordcloud_image()`.  
- `analytics/pdf.py` (новый)  
  - `create_chat_pdf(history, tone) -> bytes`.  
- `configs/`  
  - при желании: `analytics.meta.json` (порог частоты слов, стоп-слова).  
- `requirements.txt`  
  - добавить: `wordcloud`, `fpdf2` (если нет), `pandas` (если нет).  
- `logs/`  
  - `.gitignore` + пустой `README.md` (объяснить, что в проде хранить вне Git).

---

## 8) Effort Estimate (быстрый режим)
- Логирование + категоризация (rules): **~1–2 часа**  
- Табы + барчарт + таблицы: **~1 час**  
- WordCloud: **~30 мин**  
- Тон ответа (промпт): **~30 мин**  
- PDF (fpdf2) + download: **~1 час**  
- Фидбек (UI + запись): **~30–45 мин**  
**Итого:** 4–6 часов чистой работы без «глубоких копаний».

---

## 9) Risks / Open Questions
- **Assistants vs Responses API:** ваш README описывает Assistants. Формально можно оставить (поддержка до H2’2026), но экзаменаторам может понравиться ремарка «готов к Responses API».  
- **Конфиденциальность логов:** сохраняйте CSV локально и не коммитьте в Git.  
- **Категоризация правилами:** 80/20 решение. Если захотят «умнее» — включить LLM-классификацию как флаг в UI.  
- **PDF и кириллица:** для `fpdf2` подберите шрифт с поддержкой кириллицы (добавить `add_font`).

---

## 10) Next Steps (actionable checklist)
- [ ] Добавить зависимости в `requirements.txt`: `wordcloud`, `fpdf2`, `pandas`  
- [ ] Создать `logs/` и `.gitignore` для `logs/*.csv`  
- [ ] В `Interface/app.py`:  
  - [ ] `st.tabs(["Chat", "Analytics"])`  
  - [ ] `st.radio` для тона и проброс в промпт  
  - [ ] Логирование: на отправке вопроса → запись строки в CSV  
  - [ ] Кнопки 👍/👎 → обновление строки лога  
  - [ ] Вкладка «Analytics»: барчарт категорий, таблица, WordCloud  
  - [ ] Кнопка `st.download_button` для PDF (вызов `create_chat_pdf`)  
- [ ] (Опционально) Включить `classify_with_llm` и флаг «Use LLM for categorization»  
- [ ] Обновить README (скриншоты вкладки Analytics + PDF)  
- [ ] Снять короткое видео/гиф из Streamlit как демо для экзаменатора

---

## 11) Source References (links + verification date: 2025-11-05)
- Репозиторий проекта и README (структура, UI на Streamlit): GitHub — **Financial-Rag-Assistant**. Verified 2025-11-05. :contentReference[oaicite:0]{index=0}  
- Streamlit `st.tabs()` официально: Docs. Verified 2025-11-05. :contentReference[oaicite:1]{index=1}  
- Streamlit `st.download_button()` и KB «как скачать файл»: Docs. Verified 2025-11-05. :contentReference[oaicite:2]{index=2}  
- Библиотека **wordcloud**: оф. сайт/доки и PyPI. Verified 2025-11-05. :contentReference[oaicite:3]{index=3}  
- **fpdf2** для PDF: оф. доки и GitHub. Verified 2025-11-05. :contentReference[oaicite:4]{index=4}  
- OpenAI — основы промптинга/тон инструкции: Docs/Help Center. Verified 2025-11-05. :contentReference[oaicite:5]{index=5}  
- Переход на **Responses API** (контекст к Assistants): Reuters/The Verge (новости о замене). Verified 2025-11-05. :contentReference[oaicite:6]{index=6}

---

## 12) Appendix — Acceptance Mapping (критерии → как показываем)
- **Категоризация:** бейдж под вопросом + колонка в Analytics.  
- **Интерактивная аналитика:** вкладка «Analytics» с графиками/таблицей, фильтры по дате/категории.  
- **Визуализации:** WordCloud + bar chart.  
- **Тон ответа:** radio-переключатель + видимое упоминание выбранного тона в подписи ответа.  
- **PDF:** кнопка «Download PDF»; PDF содержит заголовок, N последних Q/A и итог.  
- **Фидбек:** кнопки 👍/👎 у каждого ответа + сводка полезности в Analytics.

---

### Готов приступить:  
Напиши «>», и я начну с **первого коммита** — добавлю логи и вкладку Analytics, затем по чек-листу (один код-блок за раз, как договорено).

ChatGPT: https://chatgpt.com/share/690af65c-3b58-800d-8b1f-f81251e9a4fc